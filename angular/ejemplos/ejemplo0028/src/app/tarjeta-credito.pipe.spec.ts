import { TarjetaCreditoPipe } from './tarjeta-credito.pipe';

describe('TarjetaCreditoPipe', () => {
  it('create an instance', () => {
    const pipe = new TarjetaCreditoPipe();
    expect(pipe).toBeTruthy();
  });
});
